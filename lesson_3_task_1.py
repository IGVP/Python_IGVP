"""
1. Реализовать функцию, принимающую два числа (позиционные аргументы)
и выполняющую их деление. Числа запрашивать у пользователя,
предусмотреть обработку ситуации деления на ноль (try except).

Пример:
Введите первое число: 10
Введите второе число: 0
Вы что? Пытаетесь делить на 0!

Process finished with exit code 0

Пример:
Введите первое число: 10
Введите второе число: 10
1.0

Process finished with exit code 0
"""


def my_div():
    # Задаем функцию и вводим аргументы
    # Делаем проверку на далание на 0
    try:
        arg_1 = float(input('Ведите делимое: '))
        arg_2 = float(input('Dведите делитель: '))
        return arg_1 / arg_2
    # При получении 0 в делителе выдаем ошибку
    except ZeroDivisionError:
        return 'Деление на 0 недопустимо, запустите программу ещё раз.'
    # Если введено не число выдаем ошибку
    except ValueError:
        return 'Необходимо ввести число, запустите программу ещё раз.'

# Вызываем функцию на печать
print(my_div())
